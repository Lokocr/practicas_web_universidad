let base = parseInt(prompt('Ingreso la base'));
let exponente = parseInt( prompt('Ingrese el exponente') )


const valorCalculado = Math.pow(base, exponente);
alert('Valor del exponente: ' + valorCalculado);